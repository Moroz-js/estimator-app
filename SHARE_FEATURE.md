# Функционал шаринга проектов

## Что добавлено

1. **Кнопка "Поделиться"** в EstimatorApp (шаг 3 - эпики)
   - Появляется только для сохранённых проектов
   - При первом нажатии делает проект публичным и копирует ссылку
   - При повторном нажатии просто копирует ссылку в буфер обмена
   - Текст кнопки меняется: "Поделиться" → "Скопировать ссылку"

2. **Индикатор "🔗 Публичный"**
   - Показывается рядом с индикатором сохранения
   - Кликабельный - при клике отключает публичный доступ
   - Появляется только для публичных проектов

3. **API endpoints**
   - `/api/share` - делает проект публичным (is_public = true)
   - `/api/unshare` - отключает публичный доступ (is_public = false)
   - Оба проверяют авторизацию и права доступа

4. **Страница превью `/project/[id]/preview`**
   - Доступна только авторизированным пользователям
   - Показывает проект в режиме "только для чтения"
   - Красивый дизайн с карточками эпиков
   - Отображает:
     - Название проекта
     - Тип проекта (Web/Mobile)
     - Стек технологий (в виде чипов)
     - Все эпики и задачи с оценками
     - Суммарное время по типам задач (BA, NC, DE)
     - Цветовая кодировка типов задач

## Миграция базы данных

SQL миграция уже применена в Supabase:

```sql
ALTER TABLE projects 
ADD COLUMN IF NOT EXISTS is_public BOOLEAN DEFAULT FALSE;

CREATE INDEX IF NOT EXISTS idx_projects_is_public ON projects(is_public);
```

## Как использовать

### Включить публичный доступ:
1. Откройте сохранённый проект
2. Перейдите на шаг 3 (Эпики и подзадачи)
3. Нажмите кнопку "Поделиться"
4. Ссылка автоматически скопируется в буфер обмена
5. Появится индикатор "🔗 Публичный"
6. Отправьте ссылку другому авторизированному пользователю

### Отключить публичный доступ:
1. Нажмите на индикатор "🔗 Публичный"
2. Подтвердите отключение доступа
3. Индикатор исчезнет, ссылка перестанет работать

### Скопировать ссылку повторно:
1. Если проект уже публичный, просто нажмите "Скопировать ссылку"
2. Ссылка скопируется без дополнительных запросов

## Безопасность

- Превью доступно только авторизированным пользователям
- Проверяется флаг `is_public` перед показом проекта
- Редактирование невозможно на странице превью
- Только владелец/участник проекта может включить/отключить публичный доступ
